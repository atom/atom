#!/usr/bin/env node

'use strict';

process.on('unhandledRejection', function(e) {
  console.error(e.stack || e);
  process.exit(1);
});

const { TaskManager } = require('./lib/task');
const manager = new TaskManager();
require('./tasks/task-bootstrap')(manager.subtask());
