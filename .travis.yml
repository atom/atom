git:
  depth: 10

branches:
  only:
  - master
  - /^[0-9.]+-releases$/

matrix:
  include:
    - os: linux
      dist: trusty
      env: NODE_VERSION=6.9.4 DISPLAY=:99.0 CC=clang CXX=clang++ npm_config_clang=1

install:
  - echo "nope"
script:
  - echo "nope"

notifications:
  email:
    on_success: never
    on_failure: change
