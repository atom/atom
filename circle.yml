machine:
  environment:
    XCODE_SCHEME: test
    XCODE_WORKSPACE: test

  xcode:
    version: 7.3

  node:
    version: 4.4.7

general:
  branches:
    only:
      - as-circle-ci

dependencies:
  override:
    - script/bootstrap

test:
  override:
    - script/grunt ci
