machine:
  environment:
    XCODE_SCHEME: test
    XCODE_WORKSPACE: test
    XCODE_PROJECT: test
    TEST_JUNIT_XML_ROOT: ${CIRCLE_TEST_REPORTS}

  xcode:
    version: 7.3

dependencies:
  override:
    - echo "nope"

test:
  override:
    - echo "nope"

experimental:
  notify:
    branches:
      only:
        - master
